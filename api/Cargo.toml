[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
rusqlite = { workspace = true, optional = true }

[features]
default = ["dep:rusqlite"]
server = ["dioxus/server", "dep:rusqlite"]
